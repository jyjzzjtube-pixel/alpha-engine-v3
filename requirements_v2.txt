# ═══════════════════════════════════════════════════════════════
# Coupang Partners Profit-Maximizer V2 — 전체 의존성
# ═══════════════════════════════════════════════════════════════

# ── 웹 프레임워크 ──
Flask>=3.1.0
flask-cors>=6.0.0

# ── AI 서비스 (Gemini 무료 우선) ──
google-generativeai>=0.8.0
google-genai>=1.60.0
anthropic>=0.70.0
openai>=2.20.0

# ── 미디어 처리 ──
Pillow>=11.0.0
numpy>=2.0.0
# MoviePy는 V2에서 FFmpeg 직접 호출로 대체 (기존 V1 호환용으로 유지)
moviepy>=1.0.3

# ── TTS (텍스트→음성) ──
edge-tts>=7.0.0

# ── Whisper 자막 싱크 (로컬 무료, word_timestamps) ──
faster-whisper>=1.1.0

# ── 웹 스크래핑 / 크롤링 ──
beautifulsoup4>=4.14.0
requests>=2.31.0
selenium>=4.20.0
yt-dlp>=2025.1.0
lxml>=6.0.0

# ── Anti-Ban 크롤링 방어 ──
fake-useragent>=2.0.0
browser-cookie3>=0.20.0
PySocks>=1.7.1

# ── 소셜 미디어 업로드 ──
google-api-python-client>=2.150.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.1.1
# instagrapi>=2.1.0  # 필요시 수동 설치

# ── 유틸리티 ──
python-dotenv>=1.0.0
retry>=0.9.2

# ── Google Drive ──
# google-api-python-client 위에서 이미 포함

# ── 텔레그램 봇 ──
python-telegram-bot>=20.7
